 <%= form_with(model: [ @book, @offer], local: true) do |form| %>
  <div class="form-group">
    <%= form.submit :name => 'answer', :value => 'Approve', :class => 'btn btn-info btn-sm mt-3' %>
    <%= form.submit :name => 'answer', :value => 'Reject', :class => 'btn btn-danger btn-sm mt-3' %>
  </div>          
<% end %>